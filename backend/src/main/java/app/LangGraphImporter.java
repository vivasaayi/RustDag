package app;

// Placeholder importer so backend builds without langchain4j; replace with real
// LangGraph4j import code when you add langchain4j as a dependency.

public class LangGraphImporter {
    public static Object importFrom(GraphDefinition def) {
        // TODO: convert GraphDefinition to a real LangGraph object
        return null;
    }
}
